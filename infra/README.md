# infra

Reserved for infrastructure-related assets that don't belong in
`docker-compose.yml` directly: environment-specific compose overrides,
database init scripts, reverse-proxy configs, etc. Empty at the foundation
stage — nothing here yet.
